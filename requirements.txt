lark-parser==0.12.0
transformers>=4.41.0
datasets>=2.19.0
accelerate>=0.33.0
torch>=2.1.0; platform_system != "Darwin" or platform_machine != "arm64"
# Em Macs Apple Silicon, instale torch conforme instruções oficiais:
# https://pytorch.org/get-started/locally/
peft>=0.11.0
